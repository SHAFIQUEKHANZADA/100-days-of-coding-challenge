//Q:64 combine string
function combine(age: string, number: number): string {
    return age + number
}
console.log(combine("age: ", 30));

//Q:65 Determine the remainder
function det_num(a: number, b: number): number {
    return a % b 
}
console.log(det_num(5, 2));

//Q:66 Logical and verification
function check_num (x: boolean, y: boolean): boolean {
    return x && y
}
console.log(check_num(true, false));

